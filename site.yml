---
- name: Install WordPress, MariaDB, Nginx, and PHP-FPM
  hosts: tag_Name_webserver
  remote_user: ec2-user
  become: true
  become_method: sudo

  roles:
    - common
    - mariadb
    - nginx
    - php-fpm
    - wordpress
